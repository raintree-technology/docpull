@AGENTS.md

## Claude Code

Claude Code can read the project MCP server from `.mcp.json`. The Claude marketplace plugin in `plugin/` registers the same server and adds the same research behavior as a Claude skill.
